/* Task 2 - Retrive each customer's name, country and score. (Learning about selecting only few columns to get data instead of * everything) */


SELECT 
	first_name, 
	country, 
	score 
FROM customers;